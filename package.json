{
  "name": "task-manager-master",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && cd frontend && npm install --include=dev && npm run build",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "dotenv": "^16.0.3",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0"
  }
}